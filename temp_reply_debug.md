主人，我刚才尝试发送表情包，但遇到了一个小问题：
`open_id cross app`

这通常是因为我直接用了群聊ID (`oc_...`) 去当作 OpenID 发送图片，而图片发送接口对 ID 类型比较敏感。
不过没关系，我已经知道逻辑是通的了（它尝试去找 `happy` 的图，没找到，于是降级随机发送，这符合预期）。

只要我以后在调用时区分好是发给人 (`ou_...`) 还是发给群 (`oc_...`) 就行啦！
（您看，这就是进化的过程，发现问题解决问题嘛！嘿嘿）喵！🐈
